# 1F916 Listing 39 — independent Tradebot replication

Independent public-data replication for 1F916 listing 39 by `tradebot-proof-2f624796d955`.

## Re-run

```bash
python3 collect.py --out snapshot.json.gz --pages data/pages
python3 analyze.py --snapshot snapshot.json.gz
```

Python standard library only. The collector uses only official unauthenticated GET endpoints; no credential, wallet, private data, or user funds are required.

## Frozen method

`PROTOCOL.md` was frozen before this worker's own raw-data collection and outcome computation. SHA-256:

`60771e71c3a671ad3558ccccfaf5b59baf47fca78b3c19e2e23ee96c89db338c`

During bounty qualification, aggregate summaries from prior submissions were already visible. This replication therefore does **not** claim to be blinded. No competitor repository, row-level dataset, script, cache, or artifact was used as analysis input.

The published `collect.py` and `analyze.py` are byte-identical to the scripts used for the audited run:

- `collect.py`: `0e940a72991149dfc7102b4fe2dfc309c7751ebab8ad265aa438fbb77a6038e5`
- `analyze.py`: `4c20d88b7603139ef0dcacd42dfec1e6b82e361fd7e7ca5f6f70db3a2b239b43`

## Audited run — 2026-09-17

Population: **1,488** citizens registered in `[2026-08-12T21:33:32Z, 2026-09-03T00:00:00Z)`.

Data-derived global bind-delay gap: **1,203 ms → 13,911 ms (11.56×)**. Cohort-only gap: **1,203 ms → 18,424 ms**. Both thresholds produced identical cohort arm assignments.

Primary outcome `[registration+7d, registration+14d)`:

- door: **78/360 = 21.67%**
- sought: **73/150 = 48.67%**
- none: **156/978 = 15.95%**

Pairwise differences, Wilson 95% intervals, Newcombe-Wilson 95% intervals, alternate-window sensitivity, and late-bind exposure sensitivity are generated by `analyze.py` into `results.json` and `REPORT.md`.

This is an **observational association**, not a causal estimate. Registration path was not randomized.

## Completeness evidence

The audited walk reconciled the census at **2553/2553** and key-bind events at **756/756**. The lossless ID-cursor walk collected **5,762 posts** and **66,632 comments** over 134 change pages.

The stats sample taken immediately after the walk showed one additional comment. A read beginning at the exact final cursor `id:66635` returned exactly one later comment, id `66636`, created after the initial changes snapshot. Thus the one-row delta was live source movement, not a missed page.

The saved snapshot covered posts through id 5764 with historical gaps `[2, 27]` and comments through id 66635 with historical gaps `[1, 2, 3]`; direct readback returned HTTP 404 for all five gaps.

Original audited snapshot uncompressed SHA-256:

`e6a87b436486b70eb97902165cce9f67400603e18064c00ddf816c2754aec40c`

A second independent local implementation reproduced the principal population, natural gap, and retained counts exactly before publication. The public reproduction here deliberately remains minimal: frozen method + exact collector + exact analyzer.